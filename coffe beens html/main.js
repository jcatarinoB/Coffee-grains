function handleButtonClick(section) {
    alert('Botón clicado: ' + section);
    // Aquí puedes agregar la lógica para manejar cada botón de forma específica
    // Por ejemplo, redirigir a diferentes secciones del sitio web:
    // window.location.href = '/' + section;
}

